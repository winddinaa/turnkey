import styled from "@emotion/styled";
import { theme } from "../../../core/theme/theme";

export const StyledWrapSwiper = styled.div`
  .swiper-pagination-bullet-active {
    background-color: ${theme.extend.colors.primary};
  }
`;
